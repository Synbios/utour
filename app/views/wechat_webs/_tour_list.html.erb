<% tours.each do |tour| %>
<div class="list-group-item">
  <div class="row">
    <div class="col-xs-9 col-sm-9 col-md-10" style="padding:5px">
      <h4 class="list-group-item-heading"><%= tour.name %></h4>
      <p class="list-group-item-text"><%= tour.description %></p>
    </div>
    <div class="col-xs-3 col-sm-3 col-md-2" style="padding:5px">
      <a href="/tours/1/legacy" class="btn btn-warning btn-sm" style="width: 100%; margin-bottom: 2px; color: white; padding: 5px 2px">行程</a>
      <button class="btn btn-info btn-sm" data-toggle="collapse" data-target="#<%= tour.object_id %>" style="width:100%;color: white; padding: 5px 2px">预订</button>
    </div>
  </div>
</div>

<div id="<%= tour.object_id %>" class="panel panel-default collapse">
  <% if tour.date_and_prices.empty? %>
  出团日期和价格敬请期待...
  <% else %>
  <% if inspect_right(tour) == :trade %>
  <table class="table table-striped" style="background-color: white; padding: 5px;">
    <thead>
      <tr>
        <th>日期</th>
        <th style="color: red">同行</th>
        <th>直客</th>
        <th>余位数</th>
      </tr>
    </thead>

    <% tour.date_and_prices.each do |date_and_price| %>
    <tr>
      <td><%= date_and_price.departure_date.strftime("%m.%d.%y") %></td>
      <td style="color: red">￥<%= date_and_price.trade_price %></td>
      <td>￥<%= date_and_price.retail_price %></td>
      <td><%= link_to "报名", new_booking_path(date_and_price_id: date_and_price.id) %></td>
    </tr>
    <% end %>

  </table>

  <% else %>

  <table class="table table-striped" style="background-color: white; padding: 5px;">
    <tr>
      <th>日期</th>
      <th>单人价</th>
      <th>余位数</th>
    </tr>
    <% tour.date_and_prices.each do |date_and_price| %>
    <tr>
      <td><%= date_and_price.departure_date.strftime("%m.%d.%y") %></td>
      <td>￥<%= date_and_price.retail_price %></td>
      <td><%= link_to "报名", new_booking_path(date_and_price_id: date_and_price.id) %></td>
    </tr>
    <% end %>
  </table>

  <% end %>

  <% end %>
  
  <div class="row" style="width:100%; margin: 5px 0;">  
    <a class="col-xs-4 col-xs-offset-1 btn btn-success btn-sm" style="padding: 5px">word版</a>
    <a class="col-xs-4 col-xs-offset-2 btn btn-warning btn-sm" style="padding: 5px">协议</a>
  </div>
</div>
<% end %>